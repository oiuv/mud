# AI 运行时阶段验收记录

日期：2026-09-25—26。OpenSpec：`evolve-ai-capability-runtime`；既有业务迁移基线：`2f8192a5`，共享运行时基础已提交为 `0da0c7ab`。

## 本阶段完成范围

- 固定 Hook 事件、不可变观测、受控追加上下文、工具参数重验；失败、超时和取消均有后置记录。
- 持久化候选与业务提交分离，提交后唯一终态；提交失败不提前报告成功，终态观察失败不重复提交。
- 工具发现、注册表快照、结果上限、共享预算、取消和有界等待；调用 ID 去重包含权限、受众及会话，迟到结果不注册证据。
- 自动发现业务 Skill；只注入授权目录，唯一 `skill(name, path?)` 入口；预加载复用同一边界，正文与资源 hash 在运行内固定。

主要回归：`test_runtime.py`、`test_runtime_lifecycle.py`、`test_skills.py`、`test_tools.py`、`test_source_tools.py`、`test_runtime_probe.py`。使用假模型和临时目录，不读取玩家数据。

## 基础阶段自动回归结果

本次在 Windows 执行；测试使用现有 `bin/driver.exe`，隔离驱动日志记录版本为 `fluffos 20260729-19ffcc7a-6cf257ce (Microsoft Windows)`。

| 命令 | 结果 |
| --- | --- |
| `python -m unittest discover -s ai/tests -q` | 217 项，209 通过、8 跳过，98.797 秒；跳过项为要求 Linux/flock 的启动器测试 |
| `node ai/scripts/test_lpc.mjs` | 163 项检查，0 失败 |
| `node tools/tests/test_illusion_world.mjs` | 141,287 项检查，0 失败；重启复用、7 组 LPC/Python 内容向量、异步发布/回退通过 |
| `node tools/tests/test_illusion_encounters.mjs` | 遭遇 token 基线通过 |
| `openspec validate evolve-ai-capability-runtime --strict` | 通过 |
| `openspec validate --all --strict` | 8 项通过，0 失败 |

LPC 测试在临时 MUDLIB 运行；世界测试中的“1 model call”来自假模型计数，不计入下节的真实 API 调用。地图预览仅生成在系统临时目录，未加入仓库。Windows 驱动日志提示平台不支持 eval limit，因此本轮不作为 Linux eval 成本验收，也不代替实服联调。本次没有修改 LPC 文件。

## 基础阶段真实模型链路验证

经用户授权执行一次 `python ai/scripts/verify_runtime.py --execute`，使用部署配置的 `qwen3.8-flash`。仅发送脚本生成的虚构霞门规则，不发送游戏源码、玩家历史或真实世界数据。

| 测试 | 模型调用 | 工具调用 | 耗时 | Token | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 发现 Skill → 加载正文 → 读取包内参考资料 → 回答 | 3 | 2 | 3.336 秒 | 2,004 | 通过 |
| 程序通过统一工具预加载 → 单次生成 | 1 | 1 | 1.255 秒 | 199 | 通过 |
| 合计 | 4 | 3 | 4.591 秒 | 2,203 | 两条路径均通过 |

两条路径均区分“入门贡献门槛 200”和“成功后扣除 50”；调查路径实际读取了参考文件，不因碰巧猜中答案而通过。调用和工具均记录后置事件，每次运行只有一个完成事件。用量为接口返回值；耗时是本次样本，不代表稳定延迟或成本上限。

## NPC、摘要与世界业务迁移

- 已将三类业务接入同一 Runner；专业提示词来自三个业务 Skill，检索经共享 `knowledge.search`。
- NPC 摘要、模型与检索共用根预算；验证向量/重排计数、摘要失败保留水位、关系与历史原子事务、成功重放、跨玩家隔离、澄清/未完成不记成功，以及伪造管理员字段不扩权。
- 世界保留内容键、事实 schema、队列、三次尝试、日额度与发布格式；一次新正文默认一个模型调用，已有正文/发布修复零调用。Skill 版本更新只影响新任务，已有正文及元数据不变。
- 补齐 429/Retry-After 在运行结果中的安全传递；写库失败、提交干预拒绝、停机及过期不发布正文。提交时检查当前尝试与租约，防止过期候选覆盖任务。
- 修复子 Agent 在进入阶段被拒绝时误终结父状态的边界：拒绝拥有独立运行状态及零调用预算视图，不丢弃父调用的待提交候选。

业务迁移回归包含 `test_npc_runtime.py`、`test_world.py`、`test_business_probe.py`；旧 LPC/UDP 和独立部署测试继续保留。独立部署当前需复制服务 Skill 包或配置替代目录。

最终全量离线回归：`python -m unittest discover -s ai/tests -q` 共 246 项，238 通过、8 项 Linux 专属跳过，135.002 秒；补充未知/未授权子 Agent 两类拒绝后，生命周期专项 25 项通过。LPC 通信回归 163 项、世界回归 141,287 项、遭遇 token 回归均通过；实际隔离驱动仍为 `fluffos 20260729-19ffcc7a-6cf257ce (Microsoft Windows)`。OpenSpec 本变更严格校验通过，全库严格校验 8 项通过。平台跳过和合成测试不替代实服验收。

### 合成资料真实联调

运行 `python ai/scripts/verify_business_agents.py --execute`，使用 `qwen3.8-flash`。临时角色、公开知识、历史、世界清单与数据库均由脚本生成；未访问正式玩家数据、游戏源码或已发布地图正文。向量/重排关闭，验证本地知识工具与模型循环。

最终批次结果：

| 能力 | 模型调用 | 工具调用 | Token | 结果 |
| --- | ---: | ---: | ---: | --- |
| 对话摘要 | 1 | 1 | 632 | 保留“计划拜师、尚未拜师”，提交水位 |
| NPC 检索问答 | 2 | 3 | 3,882 | 查到资料，区分二百门槛与五十扣除，成功重放无新调用 |
| 世界描写 | 1 | 1 | 1,044 | 单次生成，任务入库并发布，复用不再调用 |
| 合计 | 4 | 5 | 5,558 | 三个 Agent 均在业务提交后完成 |

摘要加问答为 10.542 秒；世界为 2.743 秒。人工核对：NPC 数值与两者含义正确，摘要没有把计划写成已完成；世界保留山岗、松树和旧路，合理补写松脂、石隙、风声。世界正文短于建议篇幅，NPC 有少量重复解释，仍属后续效果优化，不据此宣称完整质量验收。

调试过程中另执行过两批：首批结束时报告无法序列化只读 Hook 视图，未保留可核验用量，不列为验收通过（脚本上限 7 次调用）；第二批三项运行均 completed，4 次模型调用、5,571 tokens，但数值检查只认阿拉伯数字，把“二百/五十”误判，且 Windows 报告编码不正确。已用离线端到端和中文数字测试修复，再执行上述最终批次。前两批也实际消耗额度，不能把最终 4 次当作整个调试过程的总量；没有后台自动重提或正式世界重生成。

## 2026-09-26：诊断入口与数据兼容

- 业务迁移已保存为 `69e6bdf4`，未推送。之后将四个模型/检索诊断脚本接入公共 Runner 或 Tool 执行器，保留离线 BM25、召回分数、完整块展示和向量缓存测试；高条数诊断仅供本地 CLI，网络 Agent 权限不变。
- 检查共享外部请求预算、前置拒绝无实际调用、取消、唯一终态、缓存命中不再外发、错误脱敏及客户端释放；静态测试禁止诊断脚本直接调用低层模型或检索接口。
- 大结果测试发现 Hook 数据复制使用了 32 KiB 的默认解析上限，与合法工具结果上限不一致；已统一到有界的 128 KiB 复制窗口，工具仍限 64 KiB，观测回调仍不接收原始资料。新增超过上限拒绝及不泄漏测试。
- 诊断与生命周期专项 37 项通过；NPC/世界专项 44 项通过。NPC 用 SQLite 临时备份验证 `2f8192a5` 的固定 SQL 读取契约、摘要水位、关系及成功缓存重放；世界恢复验证正文、内容键、尝试次数、模型/Skill 元数据与日额度不变。该夹具不是启动旧版本实服的回退演练。
- 索引维护和世界备份/发布仍是可信管理职责，不注册成模型工具；部署要求见架构文档。本轮未修改正式配置、玩家/地图数据、LPC 或子模块，未调用真实模型。
- 本轮全量 `python -m unittest discover -s ai/tests -q`：259 项，251 通过、8 项 Linux 专属跳过，153.080 秒。OpenSpec 本变更及全库严格校验通过（8 项）；`git diff --check` 通过。此次未重跑 LPC/真实驱动回归，上一阶段的驱动结果不冒充本轮执行。

## 2026-09-26：连续无进展保护

- 诊断入口与专业执行规范已提交为 `7a7801b4`，未推送；随后完成任务 2.6。新增请求内指纹检测、配置阈值及换路径反馈，不增加评判模型、不自动重新执行工具或提交成果。
- 新增 `test_runtime_progress.py` 的 21 项用例，覆盖实际 Hook 参数、证据观察元数据、内容/hash/行变化、候选缺口、调用 ID 重放/冲突、批量调用、预算/取消、唯一终态和简单任务零工具调用；与 NPC 集成专项共 36 项通过。
- 首次全量 280 项中有 1 条旧断言失败：原 NPC 测试期待重复非法 JSON 耗满 5 次额度，新行为在第 4 次以 `incomplete / no_progress` 结束。已同步精确调用数、唯一终态和历史/关系/成功缓存均不落库的断言，不放宽业务成功条件。
- 最终 `python -m unittest discover -s ai/tests -q`：280 项，272 通过、8 项既有 Linux 专属跳过，70.861 秒。仅临时数据及假模型，未调用真实模型、未重跑 LPC/实服回归；不据此宣称源码或主 Agent 效果已验收。

## 验收边界与下一步

本记录验证共享机制、现有三项业务迁移、诊断入口及连续无进展保护，不代表整个架构重构完成。下一步实施可信入口权限装配、可配置源码范围、规则调查及主 Agent。

尚未完成：实际源码调查及至少 20 题评测、主 Agent 路由/委派、Linux 专属安全与部署项、游戏联调及临时数据升级/回退演练。真实模型调用授权不等于开放任意源码或玩家数据的读取/外发权限，实际 scope、受众仍须明确配置。不得据此勾选无限世界玩法验收、归档整个变更或自动重生成旧正文。
