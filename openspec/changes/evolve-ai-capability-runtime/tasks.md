# Tasks

## 1. 固定迁移基线与独立部署边界

- [x] 1.1 以 `2f8192a5` 为基线补齐旧报文、NPC 成功去重与世界持久任务/正文的临时数据夹具；运行现有 `python -m unittest discover -s ai/tests -v`、`node ai/scripts/test_lpc.mjs` 和 `node tools/tests/test_illusion_world.mjs`，记录结果及平台跳过项，不读取或提交真实玩家数据。
- [x] 1.2 在 `ai/main.py` 的显式装配中支持按配置选择业务，保留旧部署默认行为；测试仅注册临时能力时不加载 NPC/世界，禁用世界时不访问其清单及发布目录，不增加通用插件容器。
- [x] 1.3 复用现有路径配置，补齐独立部署所需的可选业务配置及校验；测试进程环境优先、任意 cwd、绝对/相对路径、缺少未启用业务目录，以及显式配置后不回读本库默认路径。
- [x] 1.4 新增 `docs/architecture/ai-service.md` 的边界与迁移章节，明确已实现/待实现状态，并在 `ai/README.md` 说明独立部署、单游戏实例和共享目录业务例外；用无 MUDLIB 临时部署及替代角色/知识的假模型 socket 测试验证说明。

## 2. 共享模型边界与目标驱动运行时

- [x] 2.1 扩展 `ai/src/llm.py` 的结构化响应和工具能力声明，保留 `complete_chat()` 文本兼容包装；在模型假客户端测试中验证文本、工具 ID/参数、空白、截断、用量、能力不匹配及安全错误，不静默切换模型。
- [x] 2.2 建立 Agent 定义、注册表、运行上下文、业务结果和完成验证契约；用临时 Agent 测试注册冲突、非法输入、必需配置缺失及五类终态，验证公共运行时不反向导入 NPC/世界实现。
- [x] 2.3 实现 `single` 与目标驱动 `tool_loop`，维护可见目标、事实、证据和待查项；用脚本化假模型验证多轮调查、首次无命中改查、工具失败后重规划、候选不完整继续补查及已完成停止，不记录私有思维链。
- [x] 2.4 实现请求内共享预算、并发安全预留、截止期限和取消；测试摘要、子任务、向量/重排及失败调用共用计数，循环和非法工具消耗预算，过期/取消后无新调用或迟到提交，本游戏 NPC 期限仍不超过 80 秒。
- [x] 2.5 在架构文档记录状态、完成验证和预算契约，给出直接多步与单次生成示例；以离线示例验证两种模式，明确普通任务不跨重启自动续跑、不新增通用运行数据库。
- [x] 2.6 在现有硬预算之外实现连续无进展检测与有界反馈，规范化等价工具参数并识别有效结果/资料版本变化；测试重复失败或同一完成缺口达到阈值返回 incomplete、换调用 ID 不绕过、调整查询后取得新证据可继续、首次无命中及合理重读不误停。同步运行时说明，不增加强制审稿或进展评判模型，不自动重提有副作用的任务。

## 3. 不可绕过的权限与 Hook

- [x] 3.1 实现可信入口策略及部署/Agent/父调用/受众/会话权限交集；测试伪造管理员、改写 scope、跨玩家资料、委派扩权和缓存范围冲突，默认源码及主 Agent 入口保持关闭。
- [x] 3.2 实现固定生命周期 Hook 和不可变事件视图，区分只读观测与必需干预；测试直接/委派事件关联、实际调用成功/失败/超时/取消均有一次后置事件、前置拒绝标明未执行、观察者故障隔离、必需回调超时关闭、禁止递归观测和不可变字段保护，保持固定回调顺序且拒绝不能被后续允许覆盖。
- [x] 3.3 将模型、工具及提交边界接入调用前后校验：before_model 只追加受控上下文，before_tool 仅调整声明允许的参数，后置及提交前干预不替换原始响应/证据/业务结果，终态仅观测；变更后重验参数、大小、外发、权限和预算。测试整体替换系统消息被拒、偷换路径/受众被拒、提交前取消、调用后拒绝仍计实际用量，以及需要持久化时提交完成后才发唯一 run_end、提交失败不先报成功、终态观察故障不重复提交。
- [x] 3.4 补齐脱敏轨迹与中文 Hook/安全文档；测试异常含密钥、私密历史、源码或提示词时不写出原文，明确可信回调不等于恶意代码沙箱，并验证模型客户端和数据库连接未暴露给 Hook。

## 4. 可复用工具与安全读范围

- [x] 4.1 建立受信任部署工具声明、启动发现和统一执行入口，契约包含参数/结果 schema、权限、只读性、并发安全性、超时/取消、输出上限和成本类别，首版顺序执行；测试新增临时工具无须修改中央分支、发现不等于授权、重名拒绝、参数改写后重验、结果上限、失败/取消结束记录、重复调用 ID 及同 ID 内容冲突。验证无匹配/截断/文件变化/未授权错误可区分、可恢复问题反馈 Agent、根预算耗尽或取消停止后续调用且迟到结果不交付，工具清单不含写入/删除/执行/任意网络能力。
- [x] 4.2 将现有混合知识检索封装为 `knowledge.search`，程序调用和模型调用复用同一权限与预算入口；测试 BM25/向量融合、重排回退、受众缓存隔离、外发前过滤及外部请求计数，保留原索引和生命周期。
- [ ] 4.3 实现可配置 scope 与 `source.search/read` 的有界字面量搜索、行读取和证据记录；测试默认无授权、包含/排除、外发许可、隐私文件名/计数不泄漏、扫描截断、过大文件和内容变化，输出行号/hash/读取时刻。
- [ ] 4.4 实现 Windows/Linux 的最终访问对象校验和安全失败策略；分别测试穿越、绝对/UNC/设备路径、ADS、符号链接、junction/reparse point、硬链接及替换竞争，无法验证的平台项不得以跳过冒充通过，也不得降级为不安全读取。
- [ ] 4.5 在架构文档增加工具接入及 scope 安全说明，提供不启用真实目录的中文样例；用临时目录验证样例，说明路径直接配置、授权不等于外发许可及资料提示注入不改变权限。

## 5. 专业 Skill 包与统一加载

- [x] 5.1 自动扫描配置的业务技能目录（默认 `ai/skills/`），校验元数据并按有效权限将名称和描述注入 Agent 系统提示词；实现唯一的 `skill` 工具，按技能名加载正文、按技能名和可选资源路径读取资料，直接预加载复用同一入口，不增加列举工具或 `action` 分发层。测试部署多个或新增技能仍只有一个工具入口且无中央分支改动、发现不授予权限、不预先注入全部正文、无额外模型轮次、越界资源拒绝、目录/工具可见性/加载权限一致、工具禁用时不绕过预加载且不加载开发用 `.agents/skills/`。
- [x] 5.2 实现运行内技能版本/hash 固定、引用资料审计及缺失/变更处理；测试损坏技能仅影响相关能力、中途变更不混用版本、脚本不执行、`allowed-tools` 不扩权及无隐式源码内提示词回退。验证技能正文和必需完整指导超限时明确失败，不把截断片段当作完整指导成功加载。
- [x] 5.3 建立 NPC 对话、源码调查、摘要、世界描写和主 Agent 协调五个中文技能包及正反例；测试目标/完成条件/检查清单齐全，角色由配置注入，世界 v2 合理补白语义和输出契约保持。
- [x] 5.4 在架构与业务文档记录技能维护、版本升级和效果评测方法；用固定样例验证专业提示词已外置，运行时仅保留短安全/结构协议，升级不改内容键、不重做旧正文。

## 6. 迁移已有 NPC、摘要及世界能力

- [x] 6.1 将 NPC 问答与内部摘要接入独立 Agent，检索走共享 Tool，提示词走 Skill；扩展 `ai/tests/test_npc_ai.py` 等测试人设、摘要期限、历史/关系原子提交、跨会话隔离、失败/澄清/未完成不污染数据，保持旧入口和公开知识回退。
- [x] 6.2 将世界生成接入默认单次 Agent，持久队列及 JSON 发布保留在 `world/` 业务层；扩展 `ai/tests/test_world.py` 验证一份新正文默认一次模型调用、旧正文零调用复用、内容键/事实 schema 不变、90 秒及三次尝试限制、恢复和额度不重复计数。
- [x] 6.3 审查并封闭已有业务和诊断脚本绕过公共模型/工具/策略的路径，保留受信任存储与发布职责；用冷启动、成功缓存重放、提交失败、任务恢复及回退夹具验证旧数据可读，不删除数据库或覆盖配置。验证 NPC/世界适配层在提交结果确定后结束运行、失败不提前报 completed，通用 Runner 不导入业务数据库或发布实现。
- [x] 6.4 同步根 `AGENTS.md`、`ai/README.md`、`docs/daemons/ai_client_d.md` 和无限世界 AI 文档的当前接入规范；运行旧 LPC/UDP 与世界回归，记录迁移完成范围，不替 `add-wuxia-infinite-world` 勾选尚未完成的玩法验收。

## 7. 源码调查能力与完成验证

- [ ] 7.1 在 NPC/规则问答 Agent 中接入授权源码工具、源码 Skill、待查依赖与证据引用校验；用多文件夹具验证入口定位、调用/宏/继承追踪、贡献门槛和扣除量、学习与施展区别、替代路径，不硬编码各问题答案或执行源码。
- [ ] 7.2 完成候选结果检查及玩家/管理员视图转换；测试虚构引用、过时片段、未决冲突、同名澄清、真实权限阻碍和预算耗尽，玩家提示保持游戏语境，不能用最终隐藏引用替代读前授权。
- [ ] 7.3 建立至少 20 题的固定评测集和报告工具，每题包含人工核对的结论、必要证据和预期终态；离线测试准确计分、可回答题拒答不算通过，报告正确率/完成率/无依据结论/延迟/调用数，默认禁止真实模型调用。
- [ ] 7.4 补充源码问答部署、证据快照和效果验收说明，提供受 OS 权限保护的诊断入口；用临时 scope 验证诊断和外发拒绝，明确源码版本不等于已加载对象，实际目录和真实额度需另行授权。

## 8. 主 Agent 与兼容路由

- [ ] 8.1 实现 `agent.list/invoke`，通过既有业务适配调用同一 Agent；测试授权发现、参数验证、父子权限交集、共享预算、单层顺序委派、环检测及子业务容量已满，无自回调 socket 或复制业务实现。子任务复用 Runner 并使用独立消息历史/状态、声明的模型配置与统一 skill 工具；检查实际模型消息，证明仅传必要输入且大量子任务资料、技能正文和工具过程不自动回灌父上下文，隔离不重置预算、截止时间或取消，不引入逐 Skill 执行器。
- [ ] 8.2 接入默认关闭的 `main_router` 和显式 `agent_run`，让主 Agent 检查结果、补充委派或澄清；用假模型验证简单请求由主 Agent 直接完成且子 Agent 调用次数为零，以及复杂目标的非固定分类多步协调，不因使用工具或多轮处理强制委派。世界仅受理时返回凭据不冒充完成，摘要默认不向主 Agent 开放。定义有界结果摘要、必要业务结论、成果/证据引用、限制及未完成事项的回传契约；测试条件/数值/例外不因压缩丢失，子任务成功不等于整体完成，不为协调要求回传完整过程或新增摘要模型。
- [ ] 8.3 复用持久请求缓存机制并隔离业务命名空间与完整指纹，保持旧 NPC 缓存兼容；测试并发重传、重启后成功重放、同编号不同内容、取消、迟到及超大响应，保证旧请求不隐式转主 Agent。
- [ ] 8.4 更新游戏端协议说明和独立 socket 客户端示例，说明身份信任、终态、后台受理及有限重试；用普通 Python 客户端验证无需本库对象即可接入，保留本游戏 `AI_CLIENT_D`、NPC 校验/显示职责及既有短请求语义。
- [ ] 8.5 实现请求内有界成果保存和引用交付，短结果仍可直接返回，不新增通用成果数据库或模型可见写工具；测试原成果交付不经模型重写、伪造/跨请求/跨会话/失效引用拒绝、未提交候选与世界受理凭据不冒充完成、输出超限/取消不绕过检查、持久成功响应不依赖悬空引用且重放不重新生成或提交。同步 `docs/architecture/ai-service.md`、`ai/README.md` 与根 `AGENTS.md` 的职责和上下文边界，保留世界单次生成不增调用的回归。

## 9. 跨模块验收与交付

- [ ] 9.1 运行全量 Python 离线测试、`node ai/scripts/test_lpc.mjs`、`node tools/tests/test_illusion_world.mjs` 和 `node tools/tests/test_illusion_encounters.mjs`；改动 LPC 时先执行项目格式化及 `--check`，记录实际驱动版本、失败及跳过项，不用隔离测试冒充实服验收。
- [ ] 9.2 在 Windows/Linux 分别完成独立部署、替代游戏配置、源码安全访问和启动器集成验收；验证公共运行时无游戏反向依赖、禁用模块无目录依赖及临时新能力按统一规范接入，缺少环境时明确保留未完成项。
- [ ] 9.3 取得实际 scope、受众、外发及模型总调用预算授权后，执行固定题真实模型评测和直接/委派对照；逐题人工核对核心条件、数值及证据，报告耗时和用量，未获授权不调用也不勾选完成。分别记录主/子 Agent 的模型配置、输入输出用量、上下文峰值、总调用数与端到端延迟，比较同等目标和完成质量下的隔离收益；不预先承诺节省比例，不用假模型消息隔离测试冒充真实成本结论。
- [ ] 9.4 用临时数据副本演练升级、停止在途任务和回退，核对历史/关系/去重、世界任务/额度/正文不丢失、不重生成；实际游戏中复核 NPC 回复、断线旧回复不投递、世界异步正文和玩家兜底文本，记录操作证据。
- [ ] 9.5 完成规范到测试/文档的对应检查，运行 `openspec validate evolve-ai-capability-runtime --strict` 和 `openspec validate --all --strict`；确认实现与文档一致后交付，只在实际验收通过后同步正式规范或归档，明确无限世界恢复顺序及其剩余任务，排除运行数据和凭据。
