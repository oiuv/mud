# Tasks

本次实施与验证已完成；结果及未执行的真实模型验收见 [validation.md](validation.md)。勾选代表代码及对应验证完成。按 [design.md](design.md) 保留 NPC 行为和运行数据。本次修改的每批 LPC 源文件及头文件均先执行项目格式化，再运行 --check 和适当的驱动编译。

## 1. 回归基线与服务目录迁移

- [x] 1.1 在隔离目录运行现有 npc_ai Python 测试，记录平台支持与基线；准备含角色、知识库、历史、记忆和去重记录的临时样本，验证不使用真实玩家数据或自动调用模型。
- [x] 1.2 将受版本管理的 npc_ai 源码迁到 ai，更新包导入、脚本相对路径与测试夹具；验证 main.py、模块启动、诊断及检索 CLI 可从不同工作目录使用，未覆盖本地配置和运行数据。
- [x] 1.3 同步运行数据忽略规则、.editorconfig 的 PowerShell 编码路径及 updateall 排除项；用 Git 状态、中文 help 输出和目录扫描回归验证敏感/生成数据未纳入版本管理，ai 与点号目录不被全量编译。

## 2. 公共模型调用

- [x] 2.1 从 NPC 管理器提取公共客户端、模型调用和剩余期限计算到 ai/src/llm.py，消除公共层对 NPC/知识库的反向依赖；用假客户端验证消息、模型、CHAT_EXTRA_BODY、输出上限和 max_retries=0 保持。
- [x] 2.2 将 NPC 聊天、摘要和诊断脚本接到公共调用，并保留原超时与错误展示；运行既有超时测试，另验证非 NPC 调用不受聊天整体期限硬编码限制，空白/截断/迟到响应均判失败。
- [x] 2.3 明确客户端所有权与退出清理，保持脱敏诊断；以异常和正常退出测试验证客户端关闭、日志无密钥/提示词/响应正文，config-only 不发请求。

## 3. Python 业务分发与 NPC 模块

- [x] 3.1 将 NPC 字段校验、会话锁、chat/memory/config 处理和原成功事务整体迁到 ai/src/npc/；运行原聊天、摘要、检索和存储测试，验证跨重启去重、冲突编号及失败/演示不污染历史和关系。
- [x] 3.2 为 UDP 入口增加明确类型注册及公共校验，保留旧平铺协议、默认聊天和自动编号；用真实环回 UDP 测试非 NPC 无玩家字段、未知/非字符串类型、坏 JSON/UTF-8、深层结构及超限回包。
- [x] 3.3 按业务提供有界工作容量与在途请求控制，保留 NPC MAX_WORKERS；用假慢 NPC 和独立短处理方验证池占满时不互相等待，相同请求不重复调度，冲突内容被拒绝，异常与停止均释放资源。

## 4. LPC 通用客户端与 NPC 适配

- [x] 4.1 为 ai_client_d.c 增加类型化请求、业务 mapping、回调和有限选项接口；核对 LPC 文档及 FluffOS 源码后，用隔离驱动验证无玩家调用、立即失败返回、保留字段拒绝、参数不被修改及 8 KiB 限制。
- [x] 4.2 实现公共关联校验、可选业务回包校验、有限重传和统一终态清理；注入错误来源/类型/编号、重复/迟到包及超时，验证先清理后回调、只通知一次且待处理表有界。
- [x] 4.3 新增 ai_npc_d.lpc 与宏，将玩家校验、等待提示、防重复及回复展示迁入，send_chat_request 保留薄转发并传递原 NPC 对象；验证周不通、李白、NPC 模板与影子调用的名称、输出顺序和原返回约定。
- [x] 4.4 覆盖调用方销毁、玩家对象更换、通信守护程序重载和回调异常；确认无旧回复串到新玩家、无自动重载失效对象、无遗留定时器或其他请求被阻断。

## 5. 启动、迁移与开发文档

- [x] 5.1 更新 AI 的 Windows/Linux 启动脚本、日志/PID 名称及提示；运行两平台已有启动器测试，验证各平台现有初始化流程、配置保留、知识库更新、重复启动、停止身份检查和超时保留进程行为。
- [x] 5.2 更新 run.ps1、tools/tests/test_run_windows.py 及相关游戏入口引用；验证提示自动计算 ai/start.bat 路径，start/run/restart 等游戏命令不自动启动 AI 服务。
- [x] 5.3 更新 AI README、配置示例、AGENTS.md 和 ai_client_d 文档，记录通用接口、注册方式和期限区别；README 仅保留当前功能，迁移/回退记录在本变更中；以临时旧数据执行迁移演练，确认记录与索引可复用、显式路径得到尊重且现有配置不被覆盖。
- [x] 5.4 对照无限世界已同步的规划确认依赖接口可用，检查活跃文档/脚本引用；旧 npc_ai 路径仅作为迁移或历史基线说明存在，没有另一套运行入口或未实现的世界生产路由。

## 6. 整体验收

- [x] 6.1 运行迁移后的全部 AI Python 测试及游戏 Windows 启动器回归，包含检索 CLI、诊断和知识库更新；记录真实运行的平台与结果，未具备的平台环境明确列为未验证。
- [x] 6.2 使用真实 UDP 与假模型完成 LPC→Python→LPC 闭环，覆盖普通 NPC、影子 NPC、非 NPC 短确认、服务停止及恢复；验证无真实模型费用、无玩家数据写入且现有聊天体验一致。
- [x] 6.3 对本次所有 LPC/头文件执行 node tools/format_lpc.mjs、--check、FluffOS 编译及受影响游戏流程回归；交付可复现命令和结果，确认全量编译排除 ai/。
- [x] 6.4 交付迁移、回退及可单独执行的真实 NPC 聊天验收步骤，列明真实调用是否执行；确认现有角色、历史、知识库和配置均被保留，再进入无限世界开发。
