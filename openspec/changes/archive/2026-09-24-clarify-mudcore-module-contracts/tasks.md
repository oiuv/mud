# Tasks

## 1. 固定兼容基线

- [x] 1.1 在当前 mudcore 基线上运行 `node mudcore/tests/run.mjs <实际驱动路径>`，记录框架/驱动版本、default 与 overrides 结果及日志位置；以实际退出码和完整套件结果验证，不读取真实玩家数据。
- [x] 1.2 为既有默认命令顺序、短路、参数和失败提示，以及默认中文角色创建补充基线回归；在未重构的实现上运行并确认测试确实验证原行为。

## 2. 命令阶段与 parser 边界

- [x] 2.1 在 `inherit/command.c` 增加受控阶段列表、阶段验证及 protected 处理钩子，保留旧公共入口；以默认顺序、宿主重排/覆盖、短路次数、未知/重复阶段与异常传播测试验证。
- [x] 2.2 在公共头文件增加默认启用的 parser 编译选项，保护命令 efun 引用、初始化和登录 daemon 的谓词重载；以关闭时不加载谓词对象、显式选择不可用能力时报错的测试验证。
- [x] 2.3 增加不提供表情/频道服务的 action-only 命令夹具，以及已启用服务故障夹具；验证关闭阶段完全不访问服务，开启阶段故障不被吞掉。

## 3. 最小玩家组合与可选协作

- [x] 3.1 新增 `inherit/user_base.c`、`CORE_USER_BASE` 和 `_USER_BASE`，保留原完整组合；以继承列表、别名覆盖、属性/消息/命令/存档接口编译和行为检查验证不携带战斗、状态、组队、任务或 GMCP。
- [x] 3.2 调整 `inherit/move.c` 对 GMCP 和可选持久化协作者的能力检查；通过缺席、存在、存在但抛错三类夹具验证移动/清理可用、原通知不丢失且真实错误不被吞掉。
- [x] 3.3 新增独立组合测试运行器及最小 master/命令/角色初始化夹具，在临时复制中排除未选组件；验证注册、登录、基础命令、移动、保存恢复和重连可真实执行，并确认全量测试 master 未被误用。

## 4. 角色创建策略

- [x] 4.1 从登录 daemon 抽出名称提示与校验、性别选项与提示钩子，保留默认值、输入接受方式及 `CHAR_D` 入口；用原默认注册回归和局部覆盖测试验证。
- [x] 4.2 提取受保护的角色创建收尾路径，支持空性别选项且校验畸形配置，保留身份与连接授权检查；通过无性别、错误输入、冲突选项、未认证调用和 UID 拒绝测试验证无半创建角色或身份泄漏。
- [x] 4.3 增加真实回环登录用例，覆盖非中文名称、自定义性别、无性别、无等级/HP 初始属性及存档重连；验证新角色初始化不会在恢复/重连时重复执行。

## 5. 宿主策略诊断

- [x] 5.1 新增 `inherit/host_policy.c` 及原始/可覆盖别名，按设计返回权限方法来源、状态与提醒；以默认、部分覆盖和缺失入口对象验证逐项分类，未知来源不得报告为通过。
- [x] 5.2 为诊断增加无副作用及局限性测试，包括仍允许全部调用的宿主覆盖；验证未调用权限 apply、未写文件/联网/访问数据库/切换身份，结果没有安全认证含义或敏感数据。

## 6. 框架长期规范与接入文档

- [x] 6.1 编写 `docs/architecture.md` 与 `docs/module-contracts.md`，覆盖设计指定的公共组件依赖矩阵和关键接口完整契约；逐项对照实际源码、头文件别名与可运行夹具，明确未解耦的依赖。
- [x] 6.2 更新 `docs/integration.md`、`docs/inherit.md` 及命令、登录、角色相关手册，给出默认/最小组合、parser 关闭、局部策略和诊断示例；用隔离夹具加载示例并核对签名、返回值、生命周期和失败语义。
- [x] 6.3 更新 mudcore `AGENTS.md`，明确组件依赖、机制/策略分离、可选缺席测试、兼容和通用能力保留规则；对照本变更规范逐项检查，不改变现有格式化、命名、权限和发布约束。
- [x] 6.4 同步 `README.md`、`docs/README.md`、`docs/dependency-boundary.md`、`docs/maintenance.md`、`tests/README.md` 和 `CHANGELOG.md` 未发布条目；验证链接、命令、默认值和迁移说明一致，保留 `2.0.0-dev`。

## 7. 组合验收与交付

- [x] 7.1 将独立组合套件纳入总测试入口，确保可单独运行，任一套件失败时总入口非零退出；分别运行 default、overrides、最小组合、自定义角色策略及诊断用例，核对真实通信和状态断言。
- [x] 7.2 对本次所有新增/修改的 `.c`、`.lpc`、`.h` 从宿主根目录运行 `node tools/format_lpc.mjs <实际文件列表>`，再运行同一列表的 `--check`；审查差异并重新运行受影响回归，保留可复现结果。
- [x] 7.3 运行 OpenSpec 严格校验及主/子仓库差异检查，确认默认 API、继承组合、存档字段、宿主覆盖和通用网络能力没有意外变化；核对四项 capability 的全部场景均有测试或明确文档验收证据。
- [x] 7.4 交付实现摘要、文档入口、测试命令/结果、驱动与框架基线、未验证范围；明确临时宿主不代表实际老 MUD/minimud/MyMud 验收，不自动提交、推送、发布或归档。
