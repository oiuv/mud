# Tasks

本清单是待实施事项，勾选代表代码和对应验证均完成。阶段 1–4 对应 M1，5–8 对应 M2，9–10 对应 M3。每批修改的 LPC 文件交付前均执行 `node tools/format_lpc.mjs <files>` 和 `node tools/format_lpc.mjs --check <files>`，并进行适当的驱动编译；不能等到最后才格式化。

实施前置：`refactor-ai-service` 已完成代码及 NPC 回归验收。以下 AI 任务基于 `ai/`、通用 `ai_client_d.c` 接口和公共模型调用；不重复承担目录迁移、NPC 拆分或通用通信实现。

## 阶段基线与恢复顺序（2026-09-25）

当前完成 32/50 项，先提交 M2 已实现的异步创作、持久任务、正文发布与相关回归，作为 AI 架构重构前的阶段基线。为统一后续扩展方式，暂停本变更的新功能开发，先完成独立变更 `evolve-ai-capability-runtime` 的架构规范及现有 AI 功能迁移；本清单保持未完成，不归档，不提前勾选验收。

恢复条件：NPC、摘要和世界描写按新规范接入后通过兼容回归，保留原内容键、任务、已发布正文及去重语义。恢复时先复跑本阶段基线，再处理相邻文案辨识度、8.2 完整游戏片段和 8.3 真实 NPC 并行验收，之后继续 M3；M1 剩余实服/跨平台验收仍须补齐。AI 架构草案单独提交，生成数据不随代码提交，新增真实模型调用另行明确预算。

## 1. 世界身份与可复现基础

- [x] 1.1 实现不可变世界清单的读取、初始化和摘要校验，区分测试与正式世界；验证同 ID 不同种子/目录版本被拒绝，缺失清单不静默创建随机世界。
- [ ] 1.2 实现坐标散列、固定点插值、负数 floorDiv 与局部余数；用固定输入输出向量验证正数、负数、零、边界及不同平台运算一致。
- [x] 1.3 实现有限频段高程、湿度、温度和主题场；以相反采样顺序及插入无关 random() 调用验证输出相同。
- [x] 1.4 实现坐标安全范围、边缘裁剪和有界邻域采样；验证范围极值附近没有溢出或越界出口，非法坐标不改变玩家位置。

## 2. 地貌、场景与连通网络

- [x] 2.1 建立内容表结构和第一套雾林/旧村/残寺测试内容，包括房间角色与默认文字；验证配置缺字段、重复 ID 或非法角色时给出明确错误。
- [x] 2.2 实现生态适宜度、过渡带和自然片区；导出固定范围事实，验证地貌跨区块连续，混合边界产生林缘等过渡事实。
- [x] 2.3 实现候选锚点、占地、适宜性、优先级冲突处理及模板旋转；验证不同生成顺序下同一跨区块场景只有一套身份与布局。按模板内部连接检查各区块内片段均有合法接路门径；任一片段不满足时整体拒绝候选，不裁剪场景或打穿墙体。
- [x] 2.4 实现起点平台、成对边界通行点、区块枢纽和有界路径连接；验证原点始终可进入，保留通路不与封闭建筑墙体冲突。
- [x] 2.5 实现场景接入、普通格洪泛裁剪及规范无向边出口；验证每个可进入 ROOM 接入枢纽，普通边均存在对应反向出口，跨区块结构不会残缺。
- [x] 2.6 实现稳定离境点与门的规则描述；验证起点与每组区块的离境点可达，out 始终指向武庙且不依赖 AI。
- [x] 2.7 为区块事实增加有界 LRU，分离元数据与真实 ROOM 实例；验证超过 64 块后的淘汰和重新计算结果一致，内存不随遍历持续增加。

## 3. 游戏虚拟房间与旧玩法兼容

- [x] 3.1 最小化扩展 `virtuald.c` 识别 `.lpc` 和 `.c` 模板；验证旧两/三数字坐标模板、新非数字路由和无效路径，遵守驱动扩展名优先语义。
- [ ] 3.2 新增 `d/illusion/world.lpc` 与房间模板，解析规范世界/坐标/实例路径；验证非法字段、过期实例、跨玩家进入被拒绝，正常移动可发现并加载新 `.lpc`，模板蓝图无参数加载及 updateall 不报错或登记实例。
- [x] 3.3 从现有 `maze.c` 提取共用房间与遭遇基础，保留心魔对象、强度、概率、掉落表及物品初始化；通过差异审查和新旧房间回归确认规则没有变化。
- [ ] 3.4 接入子虚道人新入口开关与规范起点加载；验证返回起点不会多出同坐标房间，两名玩家同坐标地理一致但战斗和物品分离。
- [ ] 3.5 实现实例登记、在线对象关联、退出清理和失效保护，避免新地图写入永久 startroom；实测断线原对象重连、退出重登和驱动重启后的安全位置。
- [ ] 3.6 合成新房间默认描述、实时出口和门信息，沿用普通 look 与坐标输出；在 Telnet 与现有 Web 客户端验证可读，GMCP 的 z 不泄露实例编号。

## 4. M1：无 AI 地图验收

- [x] 4.1 新增复用真实 LPC 生成器的测试命令，导出字符地图/JSON、生态与场景分布、边界口和连通报告；验证该命令可被发现且不会调用模型或实例化全部房间。
- [x] 4.2 运行固定种子正负坐标、跨区块村落、缓存淘汰与不同生成顺序回归；保存可复现命令、种子、结果摘要和代表性地图。
- [ ] 4.3 AI 服务关闭时实走“雾林 → 旧村 → 残寺 → 离境”并完成原心魔任务计数检查；验证默认地图完整可玩、战斗不变、原任务仍需二十次击杀。

## 5. AI 内容键、配置与短任务协议

- [x] 5.1 定义有限房间事实 schema、内容键编码及请求/响应状态；通过 LPC/Python 黄金向量验证同一事实得到同一键，字典顺序变化不影响键。
- [x] 5.2 增加独立的场景生成开关、内容目录、后台超时、预算等配置，复用现有聊天模型/认证；验证相对路径、环境变量优先级、无 Key 或禁用时聊天与地图回退行为。
- [x] 5.3 在统一 AI 服务注册点接入 `ai/src/world/service.py` 的 `world_describe`、`world_status`，配置独立有界短请求容量并返回确认；验证不进入 NPC 校验/工作池，无需伪造 NPC 或玩家聊天字段，旧 chat/memory/config 保持。
- [x] 5.4 让独立内容守护程序使用通用 LPC AI 请求/回调接口，为世界短请求配置容量及期限，维护有界内容状态与同键退避；注入丢包、重复确认、旧 request_id 和超过 8 KiB 请求，验证无串线、无限等待或重复叠加重试。
- [x] 5.5 在真实玩家进入钩子添加归属检查、每玩家速率控制和无内容时申请；验证编译、预载、NPC 移动、地图预览和重复 look 的模型调用数均为零。

## 6. 持久队列、模型调用与正文校验

- [x] 6.1 创建独立 SQLite 世界/任务/日用量记录与唯一键、状态索引；并发提交同键验证只有一个任务，相同键不同事实被拒绝且事务不覆盖原内容。
- [x] 6.2 实现单后台 worker、认领租约、有限重试及短事务，通过公共模型接口传入世界专用 90 秒调用限时；保持 SDK 隐式重试禁用，用故障测试验证预算包含每次尝试、网络调用不持有数据库锁且不受 NPC 整体期限硬编码限制。
- [x] 6.3 实现场景提示词和严格 JSON 输出，以规则事实为基础允许合理环境补白和文学比喻，保留地图与实际交互边界；用假模型回归确认补白原文可保存、结构安全校验不放松、提示词升级不重做旧正文，且不接入 NPC 记忆/RAG、不发送玩家姓名或聊天历史。
- [x] 6.4 实现字段、字符/字节长度、控制字符、代码块和事实引用校验；用截断 JSON、注入标记、额外出口字段、未知地标等负例验证失败结果不发布。
- [x] 6.5 实现后台让行聊天、429 退避、队列上限、日额度和存储预算；在慢模型与并发 NPC 问答测试中确认后台不占聊天工作线程，超限继续使用默认房间描述。
- [x] 6.6 实现重启后分批恢复和租约失效处理，避免与仍存活调用重叠；在 queued、running、retry_wait、ready 等状态中断服务后验证恢复与三次尝试上限。

## 7. 原子发布与房间读取

- [x] 7.1 实现完成正文先入库、后同目录原子发布 JSON 的流程，严格限制世界/坐标路径并核对清单；验证没有半写文件可见，未配置共享目录时只禁用创作。
- [x] 7.2 实现已完成未发布的重试与缺失/损坏存档的重新导出；注入入库后崩溃、替换失败和发布文件丢失，验证恢复不增加模型调用数。
- [x] 7.3 在游戏内容守护程序增加有界正文 LRU 和只读快照校验；验证错误世界/坐标/摘要、坏 JSON、超限文件回退，已保存正文在服务停止后仍可读。
- [x] 7.4 新房间 `long()` 合成本地有效正文或默认正文，再补固定交互提示；验证发布后下一次 look 看到新内容，不重载 ROOM，不重生物品或心魔，也不输出完成广播。
- [x] 7.5 增加运行数据忽略规则与一致性备份/恢复入口；验证 Git 不包含世界存档、SQLite/WAL、模型输出或凭证，恢复后同世界正文可以复用。

## 8. M2：异步创作闭环验收

- [x] 8.1 建立假模型端到端测试覆盖多玩家同键、离开后完成、重复查看、房间清理、游戏/服务重启；验证成功发布的正文保持逐字一致且不自动重做。
- [ ] 8.2 增加明确标记真实调用的调试工具，在独立测试世界对 20–30 个房间运行小预算测试；记录首次默认/后续 AI 的游戏片段、模型耗时与调用数，默认测试不调用外部 API。
  - 2026-09-25：v2 / qwen3.8-flash 已完成 20 次真实调用，19 份发布到测试世界、1 份保留待改稿；规则版与发布版各 104 项隔离驱动检查通过。完整游戏进入和后续 look 片段仍待补充，详见 validation.md。
- [ ] 8.3 与周不通聊天同时执行场景创作，检验线程隔离与供应商限流退避；记录响应和队列行为，不将供应商共享配额问题误判为已完全隔离。
- [x] 8.4 模拟 AI 停服、超时、坏回复、满队列、满磁盘和损坏存档；验证移动、look、战斗和 out 均保持可用，失败重试不突破预算。

## 9. M3：正式内容、管理和文档

- [ ] 9.1 补齐雾林、芦泽、苍岭、寒原及其过渡、适宜场景和主题意象表；用正式候选种子的地图预览验证成片分布、空间角色与结构密度，避免每房随机换主题。
- [ ] 9.2 新增管理员世界/任务状态、入口切换、创作暂停及受限诊断命令；验证普通玩家无权使用，状态查看无外部调用，生成开关不影响读已保存内容。
- [ ] 9.3 增加存档查询与修复 CLI，显示世界、坐标、状态、预算和失败原因；验证修复已完成结果不重新调用模型，失败任务重试须显式操作且仍受额度限制。
- [ ] 9.4 更新 `help/huanjing`、`ai/README.md`、配置示例和独立玩法/运维文档；逐项核对稳定地图、离境、共享目录、模型配置、备份与回退说明和实际行为一致。

## 10. 综合验证与开放准备

- [ ] 10.1 对不少于 20 个种子运行 256 × 256 范围检查，覆盖负坐标、极限边界与随机加载顺序；分别验证区块内连通、跨块边的成对性、跨块结构及起点可达，保存测试摘要。
- [ ] 10.2 记录固定机器上的冷/热区生成及正文读取耗时、LRU 容量和驱动评估消耗；验证目标并定位超标原因，不通过扩大全局限制掩盖问题。
- [ ] 10.3 抽查约 50 段相邻房间正文和三档玩家探索/遇敌/回城样本；评价自然性、地点辨识度及名称、方位、生态、实际交互承诺的一致性，区分合理补白/比喻与实质冲突，不因缺少逐项字段判废；记录收益节奏变化，实质矛盾内容隔离，平衡调整另提变更。
- [ ] 10.4 演练只关闭 AI、切回旧入口、排空新实例后回滚代码三种情况；验证在线玩家可离境、已保存正文不丢失、旧地图仍能加载。
- [ ] 10.5 对本次全部修改 LPC 文件完成项目格式化及 --check，运行相关 Python 测试、FluffOS 编译和游戏流程回归；交付明确的命令与结果，不把文档校验当成实现测试。
- [ ] 10.6 完成正式清单冻结与备份、限额与目录自检，准备小范围开放配置及检查记录；确认测试世界不会覆盖正式存档，外部服务仍为可选组件。
