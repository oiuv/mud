# 阶段实施与验证记录

日期：2026-09-25。当前为 **M1 地图加 M2 AI 创作闭环的阶段性实现，完整游戏与真实文案验收未完成**。不满足正式开放或归档条件；M3 尚未完成。任务勾选只代表该项代码及所列验证完成。

当前完成 32/50 项：M1 为 14/20；M2 为 18/20。三模型单房间对照之后，已将默认聊天模型切换为 `qwen3.8-flash`，并完成 v2 提示词的 20 房间真实试生成、人工审读和隔离驱动回读；19 份在测试世界发布，1 份保留待改稿。8.2 的完整游戏操作片段和 8.3 的真实 NPC 并行试验仍待补充，原有跨平台/完整游戏验收不因 AI 接入而自动通过。下文早期样本和过严判定保留为历史，按对应复审结论理解。

## 阶段提交复核与暂停安排（2026-09-25）

按用户确认，先提交已实现的 M2 阶段成果，作为后续 AI Agent 架构重构的兼容基线。暂停新增无限世界功能，先完成 `evolve-ai-capability-runtime` 的规范及现有 NPC、摘要和世界描写迁移，再继续相邻文案改善、8.2/8.3 及 M3；进度保持 **32/50**，不归档、不开放正式入口。

本次提交前重新执行以下检查，不沿用旧测试结果冒充本轮通过：

| 检查 | 本轮结果 |
| --- | --- |
| 本批 10 个 LPC 文件格式化及 `--check` | 0 个需改动，全部通过 |
| `ai/.venv/Scripts/python.exe -m unittest discover -s ai/tests -v` | 122 项，0 失败，8 项 Linux 启动器测试因 Windows 环境跳过，83.213 秒 |
| `node tools/tests/test_illusion_world.mjs` | 141,287 项检查，0 失败；独立驱动冷启动恢复通过；7 组 LPC/Python 向量、1 次假模型调用 |
| `node ai/scripts/test_lpc.mjs` | 163 项检查，0 失败 |
| `node tools/tests/test_illusion_encounters.mjs` | 原心魔逻辑 token 基线通过 |
| `openspec validate add-wuxia-infinite-world --strict` | 本变更通过 |
| `openspec validate --specs --strict` | 6 份正式规范通过；未完成的 AI 架构草案不在本次提交范围 |

本轮隔离驱动证据：`C:/Users/oiuv/AppData/Local/Temp/mud-illusion-5foRWj`、`C:/Users/oiuv/AppData/Local/Temp/mud-ai-lpc-uzj6pu`。没有新增真实模型调用、启动正式游戏、修改本地 `.env` 或重新生成已发布正文。代码、测试和文档纳入阶段提交；AI 架构草案、生成地图/正文、数据库、日志、临时产物及凭证不纳入。

这些结果不替代完整游戏首次进入/异步更新、真实 NPC 并行请求、跨平台及正式开放验收。恢复开发时先复跑本基线，不能因架构迁移将已有未完成项自动勾选。

## 本轮：默认模型与 20 房间真实测试

用户授权选择默认模型并继续小预算试验。将 `Settings.chat_model`、`.env.example` 和本地 `.env` 的 `OPENAI_MODEL` 同步改为 `qwen3.8-flash`；影响 NPC 问答、历史摘要和幻境文案，不改变向量/重排模型。思考模式仍关闭，两侧世界创作及公共入口未开启，也未重启正式服务。`.env.example` 的说明注释统一为中文。

选择依据是已有同房间对照中 Flash 的速度与可试看质量；Max 表达更自然，但只有一个样本，没有足够证据将其设为默认。没有比较价格，也没有对 Max 运行同批 20 房间，结论是阶段性选择，不是模型总体排名。

### 真实请求与审读

- 世界 `test-huanjing-m1`，种子 42，清单摘要沿用 `c790a774aa6c3e826f618e37b5589cf7bfed7ac9c117ef0c9f4e2586d85ffc46`，未改规则或清单。
- 隔离 FluffOS 使用真实生成器和 `d/illusion/room.lpc`，导出 6 间相邻松岗、9 间完整旧村、5 间相邻残寺的事实、出口和规则版 `long()`。不用 Python 重写地图。
- `illusion-prose-v2`，`qwen3.8-flash`，输出上限 1024 tokens。每房间只尝试一次，总上限 20；无后台 worker、无 SDK 重试，没有消耗其他排队任务。
- **20/20 结构校验成功**；平均 **3.763 秒**、中位数 **3.693 秒**、P95 **5.452 秒**（最近秩法）、最大 **6.490 秒**。计时覆盖单次生成函数及返回校验，不含排队或游戏网络延迟。
- 输入 **15,078**、输出 **2,502**、合计 **17,580 tokens**。连同此前三个对照，累计为 **23 次真实调用、19,509 tokens**，没有为不满意文案自动追加调用。
- 按场景角色、已知地标方位及实际玩法逐份审读，19 份可用于内部试看。局部岩面、松根、寒意、敌影与角力比喻不因输入未逐项列出而判废。
- `(-5,41)` 残寺空院写“此处虽无真敌”，与 `run_illusion_encounter()` 仍可能生成心魔的玩法有冲突风险，暂不发布。原文和用量保留，主库任务为 `failed / manual_review_hold_enemy_denial`，不自动重试。
- 文风尚有明显改进空间：相邻松岗开头雷同，多处直述争胜、角力；“无绝路可封”等句子像在回应约束，个别空院偏写殿内景象。它们属于本轮文风/角色聚焦问题，不能据此宣称整体质量验收通过。

### 接入、回读与数据保护

生成先进入忽略目录 `data/illusion_world/trials/v2-flash-20/`。审读后在独占进程锁下用 SQLite backup 备份主库，再原子导入本批 20 个新任务、来源及用量；拒绝覆盖已有键和发布文件。19 个任务发布到游戏读取目录，1 个待改稿任务不发布。导入没有新增模型调用；对照库保留的是同一批证据，统计不能与主库重复相加。先前 `(0,0)` 的隔离记录保持不变。

模型关闭时另启隔离驱动，确认 19 份正文由实际房间 `long()` 逐字读取、待改稿房间回退规则文案、重复查看不重复初始化物品。规则版与发布版各 **104 项检查、0 失败**；证据目录分别为 `C:/Users/oiuv/AppData/Local/Temp/mud-illusion-batch-v2-jHaAK9`、`C:/Users/oiuv/AppData/Local/Temp/mud-illusion-batch-v2-sYzyPK`。这是测试宿主和冷启动对比，不是完整游戏的首次进入/异步更新录像；8.2 暂不勾选。

规则版/发布版完整文字、事实和审读记录保存在忽略目录。示例：村口 `(-61,37)` 由规则的旧屋与古井概述，扩写为残垣、伏草、磨损门槛和南面古井；南向地标与坐标一致。玩家可从 `illusion enter test-huanjing-m1` 后向东，在 `(1,0)` 开始试看。

本轮 Python 全套回归 **122 项、0 失败、8 项 Windows 环境跳过**（Linux 启动器测试），84.179 秒；使用假模型，无外部额度消耗。补充默认值断言后，配置/NPC/超时日志共 6 项定向回归通过；OpenSpec 严格校验 7 项、`git diff --check` 通过。临时 LPC 工具已格式化、`--check` 并由实际驱动编译。运行数据、主库备份、模型结果与 `.env` 仍由 Git 忽略；没有修改 mudcore 或 FluffOS 源码。下一步先改善相邻文案辨识度，再补完整游戏与真实 NPC 并行验收，新增真实调用须另定预算。

## M2 实现与自动验证

实现：固定字节边界的房间事实/内容键、独立 world 路由及单 worker、SQLite 去重任务/租约/UTC 预算、受限正文校验、原子发布、本地正文 LRU、真实进入申请与限速。新增管理员事实导出、离线状态/修复/一致性备份恢复和显式 `--live` 诊断工具。具体操作见 [AI 创作文档](../../../docs/systems/illusion-world-ai.md)。

本节自动验证没有修改 `.env`、启动正式游戏、调用外部模型、开放公共入口或提交 Git；两侧创作默认关闭。生成数据继续忽略，不修改 mudcore 或 FluffOS。后续单独授权的真实试生成见下一节。

本轮验证：

- Python：`ai/.venv/Scripts/python.exe -m unittest discover -s ai/tests -v`，120 项，0 失败；8 项 Linux 启动器测试在 Windows 跳过。新增世界测试 19 项，最终针对事实数值范围与恢复保护又独立运行一次，全部通过。
- 世界真实驱动/UDP/假模型：141,287 项检查，0 失败；另一次独立驱动进程通过 3 项存档冷启动检查。
- 7 组 LPC/Python 事实向量：实际荒野、村落、寺院、地标方向、固定黄金值及坐标边界；改变 Python 字典顺序不改变摘要。
- 两名玩家共用同一内容键，丢弃首次正确确认、注入错误键/过期编号并重复回包，最终仅一次假模型调用；玩家离开后完成，下一次查看读取，原有物品初始化次数不变。
- 每玩家一分钟六个新键、正文 LRU 256、无回应服务十秒释放待确认；编译、预览、重复查看、重载和重启不创作。
- Python 故障覆盖：并发同键、队列/日额度/存储限制、三次尝试及退避、429、无 Key/目录、数据库不能持久受理、错误正文、发布失败、文件缺失/损坏修复、进程锁、遗留运行租约、慢模型与聊天隔离、备份恢复。
- 共用 NPC/通用 LPC AI 回归：`node ai/scripts/test_lpc.mjs`，163 项，0 失败；原心魔 token 基线 PASS。
- 本批 10 个 LPC 文件运行项目格式化并通过 `--check`，不涉及未修改的 LPC 文件。

真实驱动最终复核目录：`C:/Users/oiuv/AppData/Local/Temp/mud-illusion-S4pPYQ`；NPC 证据目录：`C:/Users/oiuv/AppData/Local/Temp/mud-ai-lpc-ySARTL`。临时数据库、正文与测试产物不提交。OpenSpec 严格校验 7 项通过；本地文档链接与 `git diff --check` 通过。Git 忽略检查涵盖正文目录、数据库、WAL 与锁文件，mudcore 工作区保持干净。

测试中修复了：已有目录下不存在文件时 `stat()` 返回空数组导致越界；同秒发布需重新检查缓存；Windows 备份连接未显式关闭导致文件占用；工厂部分注册失败时避免重复关闭已注册资源。故障样本保留在自动测试中。

限制：这些是隔离 MUDLIB 和假模型证据，不是实服全量编译、真实 NPC 同时问答、供应商限流或自然语言一致性的验收。8.2/8.3 保持未勾选，真实 API 调用需单独确认预算；假模型通过不能证明 AI 文风合格。

## M2 单房间真实试生成（2026-09-25）

用户授权范围为一个测试房间、最多一次真实调用。沿用现有 `qwen3.7-flash` 配置，端点主机 `dashscope.aliyuncs.com`；没有修改 `.env`、启动后台 worker、开启自动创作或切换公共入口。

- 世界 `test-huanjing-m1`，种子 42，坐标 `(0,0)`，房间“苍岭·松岗”。隔离 FluffOS 读取现有冻结清单及内容表，用真实 LPC 生成器导出事实；没有用 Python 重写地图算法。
- 清单摘要 `c790a774aa6c3e826f618e37b5589cf7bfed7ac9c117ef0c9f4e2586d85ffc46`；内容键 `365cbe666bce6ef39c5ea90b103ae081f1534e6febddc37fe84c6af21f5655f6`。
- `world_content.py describe` 先离线校验，再执行一次 `--live --max-calls 1`。真实尝试 **1 次**，模型调用耗时 **3.65 秒**；供应商返回输入 508、输出 148、总计 **656 tokens**。
- 输出通过结构校验、入库并发布；另一隔离驱动执行 3 项检查，确认关闭创作时仍能逐字回读正文。证据目录：`C:/Users/oiuv/AppData/Local/Temp/mud-illusion-live-facts-XuXUUC`。这不等于实服首次进入/后续 look 的操作验收。

**历史判定，已按下文复审修正**：当时因主题直述、陡峭地势和狂风判为未通过。将未明确列出的细节直接视为错误是过严审读，并非程序检测出事实冲突；现仅保留主题直述、修辞偏满的文风改进建议，不再因地形和风的补白判废。

已对该样本作一次性人工隔离：保留数据库正文和用量，将任务设为 `failed / semantic_review_rejected / published=0`；发布文件移至忽略目录 `data/illusion_world/trials/rejected/test-huanjing-m1-0_0/0_0.json`，同目录保留审读记录及变更前任务记录，便于恢复和比较。没有删除成果、重新生成或修改调用计数。这是本次维护操作，不代表已有通用审读管理功能。

隔离后验证任务不能被自动认领或重新发布；第三个隔离驱动的 3 项检查通过，确认正文读取返回规则回退信号、创作关闭且无待确认请求，证据目录 `C:/Users/oiuv/AppData/Local/Temp/mud-illusion-live-facts-YfcfgH`。配置文件哈希未改变，运行数据仍被 Git 忽略。

后续应先改进事实的可读表达及文风约束，补充本样例的离线回归；扩大真实样本需要另行确认调用预算。**任务 8.2、8.3 均不勾选，整体仍为 32/50**。

## M2 模型对照：qwen3.8-flash（2026-09-25）

用户要求换用 `qwen3.8-flash` 看效果。本次仅替换试验进程的模型，沿用上一节同一房间、同一内容键和 `illusion-prose-v1` 提示词，其他生成参数不变；模型调用前重新用隔离 LPC 驱动核对事实，未调整提示词来偏向某个模型。

- 本次真实调用 **1 次**，模型耗时 **2.39 秒**，供应商返回输入 508、输出 109、总计 **617 tokens**；未自动重试或换用其他模型。加上前一轮，两次独立授权试验累计调用 2 次。
- 使用独立数据库和发布目录 `data/illusion_world/trials/qwen3.8-flash-0_0/`，日调用上限为 1，未启动后台 worker；试验目录已存在时脚本拒绝重跑，避免意外重复收费。
- 正文通过结构校验，在对照目录内入库及发布。`result.json`、`model.log`、`review.json`、输入与数据库均保留在该忽略目录；没有写入游戏实际读取的 `data/illusion_world/content/`，未改变上一轮隔离记录或主库调用计数。
- `.env` 前后哈希相同，NPC 默认模型、自动创作和公共入口均未修改。没有对本轮正文执行实服显示验收，不把对照目录内 `published=1` 解释为已上线。

**历史审读及修正**：本样例更简洁。此前因感受性措辞、劲敌比喻和风声而拒绝的结论已撤回；这些在本段语境下属于氛围表达，没有具体新增敌人或操作目标，未发现必须判废的空间/玩法冲突。现为可用于游戏试看的候选，不自动覆盖已有存档。

结论仅针对各一份样本，不能证明 `qwen3.8-flash` 整体更快或质量更高。原提示词过度限制创作的问题已按下文 v2 修正；后续真实试验仍须授权。任务进度保持 **32/50**。

## M2 模型对照：qwen3.8-max（2026-09-25）

按用户追加授权，用 `qwen3.8-max` 对同一房间调用一次；仍保持同一事实、`illusion-prose-v1` 提示词、1024 输出 token 上限及其余生成参数。提示词 SHA-256 与 flash 对照一致：`ec03170db340fd5d13e94f6fe0cb23adb4dfb3c5bf97f87a76f0b0c1934f2aa3`。

本次模型调用耗时 **3.89 秒**，输入 508、输出 148、总计 **656 tokens**，尝试数 1。正文通过结构校验并写入独立试验目录 `data/illusion_world/trials/qwen3.8-max-0_0/`；与 flash 对照一样只在该目录发布，不接入游戏读取路径。配置文件哈希不变，未修改 NPC 模型、开启自动创作或重试。

| 模型 | 本次模型耗时 | 输入 / 输出 tokens | 本样例复审结论（替代旧判定） |
| --- | --- | --- | --- |
| qwen3.7-flash | 3.65 秒 | 508 / 148 | 保留为文风待润色对照：主题直述、修辞偏满；不是因补白违反规则 |
| qwen3.8-flash | 2.39 秒 | 508 / 109 | 可试看：简洁，感受性措辞和劲敌比喻属于氛围，未承诺实际操作 |
| qwen3.8-max | 3.89 秒 | 508 / 148 | 可试看：景物承载主题较自然；局部岩面、松根、凉意等是合理补白 |

以上仅是各一份样本，耗时不构成稳定性能结论，文风观察也不构成模型总体排名。累计为 **3 次真实调用、1929 tokens**，分属三次用户授权；主库保留首轮 1 次用量，两个对照库各记录 1 次，不合并覆盖旧任务。

三份原文和初审记录全部保留，未删除或重新生成。此前因 max 补写局部地形和凉意而判废的结论撤回，复审依据见下一节；记录的模型用量及原 `illusion-prose-v1` 来源不改写成 v2，任务进度仍为 **32/50**。

## M2 合理补白规范、提示词 v2 与复审（2026-09-25）

用户明确要求以改善游戏体验为目标，允许自然合理且有价值的细节。因此把审读标准从“是否逐项由输入列出”改为“是否自然、符合游戏语境、是否实际冲突或误导玩法”。已同步 proposal、design、narration spec、任务 6.3/10.3 与运维文档；地图生成与战斗规则没有改变。

- `illusion-prose-v2` 明确鼓励细节和比喻，解释归一化场值不是实际单位、局部岩面不等于道路状态；风声和一般感受可以烘托气氛，实际时辰变化、具体敌情和操作承诺仍需事实支持。
- 保留 JSON、字符/字节长度、控制字符、字段集合及地标 ID 校验，没有增加景物词语白名单或第二个审稿模型。事实 schema、摘要和内容键不变，提示词或模型变化不重做已有正文。
- 两份 qwen3.8 样本可作为游戏试看候选，max 的主题融景在本样例中较自然；qwen3.7 的设定直述属于文风问题。各原试验目录新增 `review-v2.json`，旧 `review.json` 保留并指向复审记录；并未改变旧生成正文或来源元数据。
- 本轮没有新增真实调用、改默认模型或自动替换游戏正文。主库仍保留首轮历史隔离状态，两个对照库保持原样；候选评价不等于已部署，也不代表 20–30 房间或相邻场景一致性验收通过。
- 离线世界测试 `ai/.venv/Scripts/python.exe -m unittest ai.tests.test_world -v`：**21 项通过**。手写测试样例覆盖岩面/松根补白、敌影比喻、风声与感受表达，逐字通过假模型生成、入库和发布；原有安全负例继续通过。另验证同时更换模型与提示词仍复用原文且保留原来源、调用数不增加。这些测试不证明真实模型必然遵守 v2。

完整 AI 回归 `ai/.venv/Scripts/python.exe -m unittest discover -s ai/tests -v`：**122 项，114 通过、8 项 Linux 平台测试跳过、0 失败**，耗时 107.011 秒。本轮仅修改 Python、文档和忽略的审读记录，没有修改 LPC，沿用此前 LPC 格式化及驱动验证证据，不宣称重新完成完整游戏验收。整体仍为 **32/50**。

## M1 已提交基线（历史记录）

## 实现范围

- 世界清单冻结、身份/摘要校验、整数地形/气候、负坐标和安全边界。
- 四类测试生态及各自的片区表、旧村/残寺九房间模板、共享地标、旋转和确定性占地冲突。
- 成对边界口、枢纽、合法道路、连通裁剪、稳定离境点及 64 区块 LRU。
- `.lpc` 虚拟路径/命令发现、个人实例隔离、原心魔逻辑提取、默认文本和管理测试入口。
- ASCII/JSON 地图诊断；`cmds/test/illusion_world.lpc` 已增加精确 Git 忽略例外，其余临时测试文件继续忽略。

跨区块场景规则已由用户确认：各区块中的场景连通片段必须具备本区合法接路门径，否则整体拒绝，不裁剪或穿墙。门径终点必须位于保留占地之外，不能通向占地内部的空洞。

本轮修复首次入场失败遗留登记；新实例失败时释放，已有实例重入失败保留。子虚道人新入口异常只显示游戏语境提示，诊断写日志。自然片区改为生态专属词表，避免寒原产生“密林”等错误事实。

## 环境与命令

实际驱动：`bin/driver.exe`，版本 `fluffos 20260729-19ffcc7a-6cf257ce (Microsoft Windows)`。保留默认 `maximum evaluation cost : 30000000`，未修改正式配置。

```sh
node tools/format_lpc.mjs <本次修改及新增的 LPC 文件>
node tools/format_lpc.mjs --check <相同文件>
node tools/tests/test_illusion_encounters.mjs
node tools/tests/test_illusion_world.mjs
openspec validate --all --strict
git diff --check
```

| 检查 | 最近结果 |
| --- | --- |
| LPC 格式化及 `--check` | 32 个文件通过 |
| 原心魔 token 摘要 | PASS；归一化函数改名后保持提取前基线 |
| 隔离驱动回归 | **140,992 项检查，0 失败** |
| 地图扫描 | 种子 42；76 区块正向检查后反向重建 76 区块，比对完整事实摘要 |
| 独立跨块村落 | `village:91:-240`，九房间完整保留，正反生成布局一致 |
| 地图导出 | 6 组 ASCII/JSON；JSON 回读及字符图一致 |
| OpenSpec 严格校验 | 7 项通过，0 失败 |
| 文档与差异 | 本次关键文档的本地链接、代表性地图比对及 `git diff --check` 通过 |

扫描区块 x=`-5..4`、y=`-3..3`，另有六个极限边界区块。额外在固定范围查找真实入选的跨区块村落，不将手工放置模板当作完整生成验收。

提交前复核证据目录：`C:/Users/oiuv/AppData/Local/Temp/mud-illusion-hXSN3o`，仍为 140,992 项检查、0 失败。临时产物可由脚本重新生成，不提交运行数据；命令和代表性摘要保留于本文件。另已确认实际测试世界清单受 `/data/illusion_world/` 忽略规则保护，该目录没有已跟踪文件。

## 自动覆盖与限制

- 身份：不可变初始化、授权拒绝、坏 JSON/摘要/类型、缺失清单、遗留 `.pending`、重载和测试/正式世界隔离。
- 算术：四组独立 BigInt 黄金向量，负坐标/极限余数、相反采样顺序和无关随机调用。
- 地图：每个可进入格接入枢纽，内部及跨块出口成对，极限无越界，边界两侧低频地形连续；2612 个带过渡描述的房间。
- 场景：重复 ID、非法角色、缺门径、占地内假门等拒绝；旋转、片段接路、完整结构及共享地标；道路不增加未声明的模板出口。
- 缓存：上限 64，淘汰后及反向访问完整事实一致；抽查完整 4×4 组和极限残块的离境点。
- 实例：两条真实环回连接配合测试宿主；实际 `feature/move.c` 拒绝跨玩家进入及非法坐标，起点复用，z=0，查看不重复初始化。
- 接入：旧 `.c` 数字模板、`.lpc` 优先、命令/别名发现、管理员权限和原遭遇基线。子虚道人修改的入口函数在隔离环境编译，**不等于完整 NPC 或实服验收**。

尚未验证另一操作系统、最低受支持驱动本身、完整游戏 `updateall /`、真实任务二十次击杀、重连/重登/重启、Web 显示及收益节奏。自动测试没有真实模型调用、生产玩家数据写入或正式世界创建；用户游戏实测另记如下。

## 用户游戏实测

2026-09-25，用户提供了完整游戏中的操作日志，测试世界为 `test-huanjing-m1`，种子 42。以下结论来自用户日志，不替代本地自动测试或其他尚未执行的验收：

- 成功初始化测试清单并进入 `/d/illusion/world/test-huanjing-m1~0~0~<实例标识>`。
- 起点与相邻坐标之间正常移动、反向返回及重复查看；拾取物品后，所示重复查看和返回记录中未重新出现已拾取物品。
- 心魔正常出现并进入战斗；这不等同于已验证二十次击杀任务计数或全部掉落规则。
- 返回起点后执行 `out`，成功到达 `/d/city/wumiao2` 武庙二楼。
- 日志开头的“指令错误”已由用户确认是误输入，不计作功能缺陷。

未记录客户端类型，不能据此宣称 Telnet/Web 均通过；日志中的不同实例标识也不能单独证明重连恢复或双玩家隔离。阶段提交不代表 M1 全部验收完成，任务勾选保持不变。

## 性能记录

提交前复核单次冷区构建 38,010 μs；76 次查询混合命中/未命中，p95 为 3,834 μs，最大 53,063 μs。此前两轮单次冷区为 22,054 μs 和 35,137 μs，存在明显波动。

这些结果**不证明冷区 p95 < 30 ms 达标**，须另做冷热分组压测并记录机器条件。当前 `eval_cost()` 差值返回 0，不能当作没有评估开销；任务 10.2 保持未完成。

## 代表性地图

种子 42，区块 `(-4,2)`，房间范围 x=`-64..-49`、y=`32..47`，北在上：

```text
......+.........
......+.........
......+.........
+.....+.........
+.....+.........
+.....+.........
+.....+.........
++....+.........
.+++++++++++++++
...+++++........
..SSS..+........
.+SSS+++........
..SSS..+........
...+++++........
.......+........
.......++++++...
```

`S` 旧村，`+` 道路，`.` 荒野。旧村锚点 `(-61,36)`，残寺锚点 `(-4,41)`。额外跨块旧村 `(91,-240)` 覆盖 `(5,-16)`、`(5,-15)`，两片段均从合法门径接路。

起点区块完整事实测试摘要：`566f51a7b3e9caa1a9cff6416fbfe017ad6b998ff71333eb88455a8e4189d512`。这是地图测试用递归排序摘要，不是 AI 内容键规范。

## 剩余验收

1. 任务 1.2：另一平台运行同一黄金向量；当前仅 Windows。
2. 任务 3.2：完整游戏全量编译与蓝图加载；隔离环境已检查蓝图无实例/掉落。
3. 任务 3.4–3.6：子虚道人真实入口、两玩家战斗/物品隔离、重连重启及 Telnet/Web。已有自动覆盖不能替代实服步骤。
4. 任务 4.3：关闭 AI 后实走雾林、旧村、残寺和离境，验证原任务二十次击杀。种子 42 起点是苍岭；用户已验证局部移动和起点离境，尚未完成指定探索路线和任务计数。

操作见 [无限世界文档](../../../docs/systems/illusion-world.md#游戏验收与回退)。公共入口保持关闭，不将阶段提交描述为整个功能完成，不提前归档。
