#include <ansi.h>
inherit ITEM;

void setup()
{}
void create()
{
      set_name(CYN "判官笔" NOR, ({"bi"}));
      set("unit", "支");
      set("value", 0);
      set_weight(200);
        
      setup();
}
